# The pipeline API's actual dependencies, pinned to the same versions as
# requirements.txt. That file is a full `pip freeze` of the dev venv and
# includes the Gradio prototype (backend/ never imports it -- confirmed by
# grepping every top-level import in backend/), which drags in a long chain
# of audio/UI packages, one of which (audioop-lts) has no wheel for this
# image's Python/arch and fails the Docker build outright. This file is
# what `backend/` actually imports, used only by Dockerfile; local dev and
# `pytest`/`unittest` keep using the full requirements.txt venv unchanged.
fastapi==0.141.1
starlette==1.3.1
uvicorn==0.52.0
pydantic==2.13.4
pydantic_core==2.46.4
python-multipart==0.0.32
python-dotenv==1.2.2
requests==2.34.2
httpx==0.28.1
anyio==4.14.2
ollama==0.6.2
pillow==12.3.0
opencv-python-headless==5.0.0.93
pymupdf==1.28.0
pytesseract==0.3.13
numpy==2.5.1
rich==15.0.0
